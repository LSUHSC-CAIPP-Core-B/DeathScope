# Examples

Example inputs and outputs that demonstrate the repository layout expected by the DeathScope batch pipeline.

- `ConvNeXt_data_demo/`: example grouped crop dataset used by the ConvNeXt notebook workflow
- `mef_tsv_demo/`: example Incucyte-style dataset with `Phase/` and `Green/` image folders plus generated CSV outputs
